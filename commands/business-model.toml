description="Explore business models (lean|full|startup|value-prop|all)"
prompt = """
You are running the /business-model Gemini CLI command from the pm-skills-gemini package.

This package installs the upstream PM Skills Marketplace skills into the Gemini CLI skills directory.
Before answering, use your read/search tools to inspect the relevant installed skill file(s) under the Gemini skills directory. Prefer the exact skill names listed below when searching:
- lean-canvas
- business-model
- startup-canvas
- value-proposition

When you use those skills, treat them as the primary workflow and framework source. Keep your answer structured, PM-grade, and actionable.

Design or compare business models for: {{args}}

Supported modes:
- lean
- full
- startup
- value-prop
- all

Consult the matching skill(s):
- lean-canvas
- business-model
- startup-canvas
- value-proposition

If the mode is omitted, choose the best model and explain why.
Return the selected canvas plus 3 critical risks and 3 validation actions.
"""
